{# Emphasis / thesis statement #}
<div class="slide flex flex-col items-center justify-center text-center px-16 bg-gray-50" id="slide-{{ slide._index }}">
    <div class="slide-body text-gray-900 leading-relaxed max-w-[80%] font-medium" style="font-size: clamp(2.5rem, 4.5vw, 4rem);">
        {{ slide.body_html | safe }}
    </div>
</div>
